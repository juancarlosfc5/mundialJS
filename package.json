{
  "name": "mundial-js",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "api:dev": "json-server --watch src/data/db.json --port 3000",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "json-server": "0.17.4",
    "vite": "^8.2.0"
  }
}
